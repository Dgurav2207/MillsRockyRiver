server:
 port: 8070
eureka:
 instance:
  hostname: "localhost"
 client:
  fetchRegistry: false
  registerWithEureka: false
  serviceUrl:
   defaultZone: https://${eureka.instace.hostname}:${server.port}/eureka/
